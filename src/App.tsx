/* eslint-disable @typescript-eslint/no-unused-vars */

// @ts-expect-error This import make `vite build` fail
import { MonacoVscodeApiWrapper } from "monaco-languageclient/vscodeApiWrapper";

export default function App() {
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
}
